@using Lab25.Models;
@{
    ViewBag.Title = "Add";
}

<h2>Item added young trainer!</h2>

Cart:
<ul>
    @foreach (Item b in (List<Item>)(Session["Cart"]))
    {
        <li>@b.Name cost @b.Price</li>
    }
</ul>

<div>
    @Html.ActionLink("Back to Poké Mart", "Index")
</div>